name: tower_defense
description: "A simple tower defense game written in Flutter Flame"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: ^1.32.0
  flame_test: ^2.0.3
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  test: ^1.25.15

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
